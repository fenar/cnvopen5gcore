apiVersion: v1
kind: LimitRange
metadata:
  name: container-mem-limit-range
spec:
  limits:
  - default:
      memory: 512Mi
      cpu: "300m"
    defaultRequest:
      memory: 256Mi
      cpu: "200m"
    type: Container
